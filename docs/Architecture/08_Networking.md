# Networking Architecture

## Status

Future scope.

Networking design will define local discovery, remote access, authentication, encryption, and clear user consent before any networked capability is introduced.
